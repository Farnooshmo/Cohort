/**
 * Extension
 * 1."STRING" == "string" is false. It is case sensitive!
 * "STRING".toLowerCase == "string"
 */
//2.  Not simply (we can't just use .toInt). The compiler doesn't know that "one" is the same as 1. We would need to look at mapping to be able to do this, hold this thought.
//3. The same as above.
//4.
 1.toString // it is a string not 1.
 //5.
"string".toUpperCase
//6.
"string".capitalize
